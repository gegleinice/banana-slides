# Production env template for Docker deploy.
# Copy to .env.prod before `npx profy deploy`.

# App server secrets / upstream credentials
# Example:
# OPENAI_API_KEY=

# Optional platform overrides
# PLATFORM_BASE_URL=https://profy-test.agentspro.cn
